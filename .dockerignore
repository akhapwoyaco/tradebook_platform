# Ignore Git-related files and temporary files
.git
.gitignore
*.log
*.sqlite3
*.db
.env
.DS_Store # macOS specific

# Python-specific ignores
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
*.egg-info/
.pytest_cache/
.coverage
.mypy_cache/
.ipynb_checkpoints/
.vscode/

# Project-specific ignores for large or non-essential files in the image
data/        # If raw data or large processed data is not needed inside the image
models/      # If pre-trained models are very large and will be mounted or downloaded at runtime
logs/        # Logs generated by the application
reports/
notebooks/

# Docker specific ignores (if building with a specific Dockerfile)
# Dockerfile # Uncomment if you have multiple Dockerfiles and this one is for 'Dockerfile.gpu'
# docker-compose.yml